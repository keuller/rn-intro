import React from 'react';
import styles from './AppStyles';
import { ScrollView, View, Text } from 'react-native';

const App = () => (
      <ScrollView style={styles.scrollView}>

            <Text style={styles.titulo}>Ola Pos-Univem</Text>

      </ScrollView>
);

export default App;
